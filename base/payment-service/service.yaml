apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: klikgo
spec:
  selector:
    app: payment-service
  ports:
    - protocol: TCP
      port: 8003
      targetPort: 8003
  type: ClusterIP